/**
 * LIMINAL OS – Map Generation & Visualization Module (DEPRECATED / REDIRECT)
 *
 * This file has been split into two smaller, easier-to-manage modules:
 *
 * 1. mapgenerator.js - Handles BFS generation of the graph, connectivity checks,
 *                       and the global force-directed layout computation.
 *
 * 2. mapgraph.js      - Handles the SVG rendering, user interaction, zooming,
 *                       panning, and the sidebar resizer controls.
 *
 * Both modules are included in index.html.
 */
